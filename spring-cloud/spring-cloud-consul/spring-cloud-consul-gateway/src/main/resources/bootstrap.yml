spring:
  application:
    name: gateway-service
  cloud:
    consul:
      config:
        enabled: true
        prefix: configuration
        defaultContext: common
        data-key: bootstrap
        format: YAML